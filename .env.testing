# --- Configuração Geral da Aplicação ---
APP_NAME=Laravel
APP_ENV=testing
APP_KEY=base64:bo35wxM5/QRHNdaDwA03Nssgsr6ZDAvSnWDlPuHK+N4=
APP_DEBUG=true
APP_URL=http://localhost

# --- Base de Dados (A parte mais importante) ---
DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# --- Sessão, Cache, Fila ---
# Para testes, é melhor usar 'array' ou 'sync' para evitar interações
# com a base de dados ou ficheiros, tornando os testes mais rápidos e isolados.
SESSION_DRIVER=array
CACHE_STORE=array
QUEUE_CONNECTION=sync

# --- Outras Configurações (geralmente não são críticas para testes) ---
APP_TIMEZONE=Europe/Lisbon
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US
BCRYPT_ROUNDS=4 # Usamos um valor baixo para acelerar a criação de utilizadores nos testes
LOG_CHANNEL=null # Desativamos os logs durante os testes para não encher o ficheiro

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
MEMCACHED_HOST=127.0.0.1
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# --- Chaves de Serviços (Opcional, mas boa prática) ---
# Se os seus testes precisarem do Stripe, copie as chaves de teste aqui.
STRIPE_KEY=pk_test_...
STRIPE_SECRET=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...